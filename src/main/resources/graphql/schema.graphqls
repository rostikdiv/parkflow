type Query {
  parkingLots(bbox: BboxInput): [ParkingLot!]!
  parkingLot(id: ID!): ParkingLot
  availability(lotId: ID!, from: String!, to: String!): [SpotAvailability!]!
  myReservations(status: String, page: Int = 0, size: Int = 10): ReservationPage!
}

type ReservationPage {
  content: [Reservation!]!
  totalElements: Int!
  totalPages: Int!
  number: Int!
  size: Int!
}

type Subscription {
  spotStatusChanged(lotId: ID!): SpotStatusEvent!
}

type SpotStatusEvent { 
  spotId: ID!
  status: String!
  at: String! 
}

input BboxInput {
  minLng: Float!
  minLat: Float!
  maxLng: Float!
  maxLat: Float!
}

type ParkingLot {
  id: ID!
  name: String!
  address: String!
  latitude: Float!
  longitude: Float!
  type: String!
  hourlyRate: Float!
  opensAt: String!
  closesAt: String!
  timeZone: String!
  status: String!
}

type SpotAvailability {
  spotId: ID!
  code: String!
  type: String!
  isAvailable: Boolean!
  layoutX: Float!
  layoutY: Float!
  bookedUntil: String
  isAnomaly: Boolean!
}

type Reservation {
  id: ID!
  spotId: ID!
  spotCode: String!
  lotId: ID!
  lotName: String!
  userId: ID!
  licensePlate: String!
  startTime: String!
  endTime: String!
  status: String!
  totalPrice: Float!
}
